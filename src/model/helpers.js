const { default: Patch } = require("./patch");

/**
 *
 * @param {[Patch]} patches
 * @param {[Number]} whiteValues
 * @param {Number} xRes
 * @param {Number} yRes
 */
function bakePatches(patches, whiteValues, xRes, yRes) {
  canvas = undefined;
}
